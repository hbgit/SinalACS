name: sinalacs_backend
description: Backend Serverpod do SinalACS.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  crypto: ^3.0.3
  serverpod: ^1.2.0
  postgres: ^2.6.0
  mqtt_client: ^10.0.0


dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
