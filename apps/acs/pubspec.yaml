name: sinalacs_acs
description: Aplicativo do ACS do SinalACS.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  connectivity_plus: ^5.0.0
  mqtt_client: ^10.0.0
  geolocator: ^12.0.0
  google_maps_flutter: ^2.6.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
